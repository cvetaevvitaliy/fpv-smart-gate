# nRF52840 SMART GATE board configuration

if BOARD_NRF52840_SMART_GATE

config BOARD
	default "nrf52840_smart_gate"

if USB

config USB_NRFX
	default y

config USB_DEVICE_STACK
	default y

endif # USB

config BT_CTLR
	default BT

endif # BOARD_NRF52840_SMART_GATE
